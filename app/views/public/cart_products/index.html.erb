<h1>ショッピングカート</h1>
<p>カートを空にする</p>

<table border="1">
  <tr>
    <td>商品名</td>
    <td>単価（税込）</td>
    <td>数量</td>
    <td>小計</td>
    <td></td>
  </tr>
  <tr>
    <% @cart_products.each do |cart_product| %>
    <td>画像：<%= cart_product.product.name %></td>
    <td><%= cart_product.product.price %></td>
    <td><%= form_with(model: @cart_product, class: 'js-form') do |f| %>
    <%= f.select :quantity, options_for_select((1..20).to_a), include_blank: "個数選択" %>
    <% end %></td>
    <td><%= cart_product.product.price*cart_product.quantity %></td>
    <td><%= link_to"削除する", cart_product_path(cart_product), method: :delete %></td>
  </tr>
  <% end %>
</table>

<p><%= link_to "買い物を続ける", products_path %></p>
<p>合計金額</p>

<p><%= link_to "情報入力に進む", new_order_path %></p>